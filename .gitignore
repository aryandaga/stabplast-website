# Dependencies
node_modules/

# Build output (generated by CI; not committed)
_site/

# Reference-only material — NOT part of the deployable site
# premium Chemid WordPress theme (visual reference only; do not redistribute)
chemid/
# original source-image dump; build uses src/assets/ instead
website/

# Local tooling / editor
.claude/
.DS_Store
*.log
npm-debug.log*
